import React from "react";

export const Boton = ({ textButton }) => {
  {
    return (
      <a class="btn btn-primary btn-lg" href="#" role="button">
        {textButton}
      </a>
    );
  }
};

export default Boton;
